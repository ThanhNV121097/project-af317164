# PostgreSQL connection string. Runtime injects this value.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# HTTP listen port. Runtime injects this value.
PORT=8080

# Optional fallback HTTP listen port when PORT is unset.
APP_PORT=8080
