# Local PostgreSQL user for docker compose.
POSTGRES_USER=app

# Local PostgreSQL password for docker compose.
POSTGRES_PASSWORD=app_secret

# Local PostgreSQL database name for docker compose.
POSTGRES_DB=app

# Host port exposed by backend.
BACKEND_PORT=8080

# Host port exposed by frontend.
FRONTEND_PORT=3000

# Browser-facing backend base URL for frontend build.
NEXT_PUBLIC_API_URL=http://localhost:8080
